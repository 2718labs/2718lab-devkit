# BK-05 MCP Integration

Owner: terra-mcp
Depends on: BK-02, BK-04

## Goal

Expose generic orchestration and Bugkiller operations through the existing official FastMCP server.

## Context

Read `contracts/orchestrator-api.md` and `contracts/host-boundaries.md`. Preserve `mcp.server.fastmcp`, `@mcp.tool()` and clean stdio.

## Write Scope

- `mcp-tools/server.py`
- `mcp-tools/tests/test_mcp_contract.py`

## Steps

1. Write failing source/registration contract tests.
2. Add thin JSON wrappers for workflow tools and Bugkiller case/evidence/approval tools.
3. Keep subprocess execution out of orchestration tools; errors use stable safe codes.
4. Resolve data path from explicit environment/PLUGIN_DATA/CODEX_HOME, never plugin cache.

## Acceptance

Run MCP contract tests and `python skills/mcp-server-dev/scripts/validate_mcp_server.py mcp-tools`; zero errors, no direct `print()` in stdio server.

## Return

Changed files, tool list, validation output and environment/path assumptions.
